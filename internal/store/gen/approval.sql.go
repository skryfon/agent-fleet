// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: approval.sql

package db

import (
	"context"
)

const insertApproval = `-- name: InsertApproval :one
INSERT INTO approval (subject_kind, subject_ref, subject_sha256, decision, actor, note)
VALUES ($1, $2, $3, $4, $5, $6)
RETURNING id, subject_kind, subject_ref, subject_sha256, decision, actor, decided_at, note
`

type InsertApprovalParams struct {
	SubjectKind   string  `json:"subject_kind"`
	SubjectRef    string  `json:"subject_ref"`
	SubjectSha256 string  `json:"subject_sha256"`
	Decision      string  `json:"decision"`
	Actor         string  `json:"actor"`
	Note          *string `json:"note"`
}

// POST /v1/approvals (development-plan.md §4/§3). No update path — every
// decision is its own row, per the same append-mostly instinct the `event`
// table follows (a re-decision is a new approval, not a mutated one).
func (q *Queries) InsertApproval(ctx context.Context, arg InsertApprovalParams) (Approval, error) {
	row := q.db.QueryRow(ctx, insertApproval,
		arg.SubjectKind,
		arg.SubjectRef,
		arg.SubjectSha256,
		arg.Decision,
		arg.Actor,
		arg.Note,
	)
	var i Approval
	err := row.Scan(
		&i.ID,
		&i.SubjectKind,
		&i.SubjectRef,
		&i.SubjectSha256,
		&i.Decision,
		&i.Actor,
		&i.DecidedAt,
		&i.Note,
	)
	return i, err
}
