// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: run_inbox.sql

package db

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const claimNextRunInbox = `-- name: ClaimNextRunInbox :one
UPDATE run_inbox
SET delivered_at = now()
WHERE id = (
    SELECT id FROM run_inbox
    WHERE run_id = $1 AND delivered_at IS NULL
    ORDER BY id
    LIMIT 1
    FOR UPDATE SKIP LOCKED
)
RETURNING id, run_id, kind, payload, created_at, delivered_at
`

// Claims (marks delivered_at) and returns the oldest still-pending row for
// run_id, or no rows if the inbox is empty. FOR UPDATE SKIP LOCKED so two
// concurrent long-poll requests for the same run (a redelivered HTTP retry)
// never claim, and therefore never deliver, the same row twice.
func (q *Queries) ClaimNextRunInbox(ctx context.Context, dollar_1 pgtype.UUID) (RunInbox, error) {
	row := q.db.QueryRow(ctx, claimNextRunInbox, dollar_1)
	var i RunInbox
	err := row.Scan(
		&i.ID,
		&i.RunID,
		&i.Kind,
		&i.Payload,
		&i.CreatedAt,
		&i.DeliveredAt,
	)
	return i, err
}

const enqueueRunInbox = `-- name: EnqueueRunInbox :one
INSERT INTO run_inbox (run_id, kind, payload)
VALUES ($1, $2, $3)
RETURNING id, run_id, kind, payload, created_at, delivered_at
`

type EnqueueRunInboxParams struct {
	RunID   uuid.UUID `json:"run_id"`
	Kind    string    `json:"kind"`
	Payload []byte    `json:"payload"`
}

// Durable queue entry for a worker_question/worker_report handed to a
// specific run (M5, development-plan.md §5 ask_orchestrator/
// report_to_orchestrator) — GET /v1/runs/{id}/inbox's long-poll claims
// these via ClaimNextRunInbox below, in addition to its existing run.state
// derived "cancel" delivery.
func (q *Queries) EnqueueRunInbox(ctx context.Context, arg EnqueueRunInboxParams) (RunInbox, error) {
	row := q.db.QueryRow(ctx, enqueueRunInbox, arg.RunID, arg.Kind, arg.Payload)
	var i RunInbox
	err := row.Scan(
		&i.ID,
		&i.RunID,
		&i.Kind,
		&i.Payload,
		&i.CreatedAt,
		&i.DeliveredAt,
	)
	return i, err
}
